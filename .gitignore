# Dependances
node_modules/

# Builds
dist/
src-tauri/target/

# Cle privee de signature des mises a jour : NE JAMAIS COMMITER.
# Elle vit en secret GitHub (TAURI_SIGNING_PRIVATE_KEY) et en local hors du repo.
*.key
*.key.pub
cockpit-updater.key*

# Secrets applicatifs (cle OpenAI importee au premier lancement)
secrets.json

# tmux statique embarque : construit par scripts/build-tmux-static.sh (CI), jamais commite
src-tauri/resources/bin/tmux

# Reglages locaux Claude Code (specifiques a la machine)
.claude/settings.local.json

# Specs et plans de travail : utiles en local, sans interet pour qui clone le projet.
docs/superpowers/

# Divers
*.log
.DS_Store
.claude/issues-vues.json
